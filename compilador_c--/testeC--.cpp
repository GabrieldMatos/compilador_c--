programa

int soma(int a, int b){     /*funcao que soma dois numeros*/
    return a + b;
}

int maior(int c, int d){ /*funcao que retorna o maior valor entre 2*/
    if(a >= b){
        return a;
    }
    else{
        b;
    }
}

float mult(float e, float f){
    return e*f
}

int main(){
    int x = 0;
    int y = 5;
    float t, q;
    int vet[5];
    int soma = 0;
    while(x != y){
        vet[x] = maior(x, y);
        x = x + 1;
    }
    float result = mult(t,q);
    return 0;
}